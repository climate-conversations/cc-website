class Sync {
	async processGuest(payload) {
		// Format dates

		// Sync Kepla
			// Find / Create Facil record
			// Find / Create Host
			// Create Participant
				// Create participant
				// Find facil user
				// Assign participant to facil user
			// Find / Create conversation
				// Assign host to conversation
				// Assign facil to conversation
			// Assign participant to conversation
			// Assign facil user to conversation

			// Mark record kepla synced

		// Sync FTL
			// Find / create facil
			// Find / create host
			// Find / create conversation
			// Find / create guests

			// Mark record FTL synced
	}
}
